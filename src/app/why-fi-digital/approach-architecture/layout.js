export const metadata = {
  title: "AI Architecture & Approach | Model-Agnostic | FI Digital UK",
  description: "Model-agnostic, multi-cloud AI architecture. Claude, GPT-4o, Gemini, deployed across AWS and Azure with enterprise security and observability.",
};

export default function ApproachArchitectureLayout({ children }) {
  return <>{children}</>;
}
