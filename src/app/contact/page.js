import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact & Pricing | FI Digital UK",
  description: "Professional AI & Zoho consultation in London. Transparent pricing for AI agents, ERP implementation, and digital worker deployment in the UK.",
};

export default function ContactPage() {
  return <ContactClient />;
}
