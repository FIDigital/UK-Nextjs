export const metadata = {
  title: "Finance Automation Agents | FI Digital UK",
  description: "AI agents for invoice processing, expense categorisation, reconciliation. Integrated with Xero, QuickBooks, Sage, Zoho Books. 99.2% accuracy.",
};

export default function FinanceAgentsLayout({ children }) {
  return <>{children}</>;
}
